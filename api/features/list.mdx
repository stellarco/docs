---
title: "List Features"
description: "GET /features"
---

# List Features

Returns all features in the workspace, with pagination.

## Request

```
GET /api/v1/features
```

**Query parameters:**

| Param | Type | Description |
|-------|------|-------------|
| `page` | integer | Page number (default: 1) |
| `per_page` | integer | Results per page (default: 25, max: 100) |
| `status` | string | Filter by status name |
| `assignee_id` | string | Filter by assignee ID |
| `tag` | string | Filter by tag name |

## Response

```json
{
  "data": [
    {
      "id": "feat_001",
      "title": "Add dark mode",
      "description": "Support system and manual dark mode...",
      "status": "Planned",
      "assignee": {
        "id": "usr_abc123",
        "name": "Alice Chen"
      },
      "tags": ["frontend", "ux"],
      "properties": {
        "priority": "High",
        "effort": 5
      },
      "created_at": "2025-01-15T10:00:00Z",
      "updated_at": "2025-01-20T14:30:00Z"
    }
  ],
  "meta": {
    "total": 142,
    "page": 1,
    "per_page": 25
  }
}
```
